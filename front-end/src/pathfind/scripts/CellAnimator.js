export default class CellAnimator {
  static bounce(DOM_cell) {
    const tower_left = DOM_cell.querySelector('.tower.left');
    const tower_right = DOM_cell.querySelector('.tower.right');
    const top = DOM_cell.querySelector('.top');

  }
}